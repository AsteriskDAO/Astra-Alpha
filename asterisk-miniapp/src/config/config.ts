
const config = {
    server: {
        // TODO: update to production config when needed 
        // self and tg mini apps require https, so we need to use a production server or set up a tunnel for future development
        url: 'https://asterisk-health-profile.onrender.com'
    }
}

export default config
